# MambaStock 论文关键分析

> Shi, Z. (2024). *MambaStock: Selective state space model for stock prediction*. arXiv:2402.18959.
> 来源文件：`/home/xieguiawu/Downloads/MambaStock Selective state space model for stock prediction/AI中译MambaStock.md`

---

## 一、数据

| 项目 | 内容 |
|------|------|
| **数据来源** | Tushare Pro (www.tushare.pro) — 中国 A 股免费公开数据集 |
| **股票** | 4 只银行股 |
|  | 600036.SH — 招商银行 |
|  | 601288.SH — 中国农业银行 |
|  | 601328.SH — 交通银行 |
|  | 601988.SH — 中国银行 |
| **特征**（15维） | 开盘价、最高价、最低价、收盘价、成交量、成交额、换手率、量比、PE、PB、PS、总股本、流通股本、自由流通股本、总市值、流通市值 |
| **训练/测试划分** | 测试集固定为 300 个样本（无论训练集大小） |
| **问题** | 测试集大小的固定设计与时间序列的时序性质不匹配；未说明训练集大小；未说明验证集使用 |

**评价：** 仅 4 只银行股，来自同一行业（银行业），同质性高，结论泛化性极差。数据来源 Tushare 本身没有问题，但样本量严重不足。

---

## 二、模型架构

### 整体结构

```
输入 (B, L, 15维特征)
  ↓
Mamba 层 (状态维度 N=16)
  ↓
全连接层 (降维至 1)
  ↓
tanh 激活 (输出在 (-1, 1) 之间)
  ↓
预测价格变动率 (return)
```

### Mamba 核心参数

- 状态空间维度 N = 16
- 输入→输出：B, C, Δ 通过全连接层从输入 X 动态学习（时变选择机制）
- 离散化方式：零阶保持 (ZOH)
  - Ā = exp(ΔA)
  - B̄ = (ΔA)⁻¹(exp(ΔA) - I) · ΔB
- 扫描模块 (S6)：对输入序列每个时间步滑动窗口应用可学习变换

### 预测目标

**价格变动率（return）**，而非绝对价格。公式中未明确给出，但从 tanh 输出在 (-1,1) 可推断是某种归一化的变化率。

---

## 三、训练配置

| 参数 | 值 |
|------|----|
| 优化器 | Adam |
| 学习率 | 0.01（固定，无调度） |
| 训练轮数 | 100 |
| GPU | NVIDIA GTX 3060 (12GB) |
| 损失函数 | MSE |
| 批量大小 | **未报告** |
| 序列长度 L | **未报告** |
| 早停/正则化 | **未提及** |
| 学习率衰减 | **无** |

**评价：** 学习率 0.01 且无调度对于 Mamba 结构偏高；100 epoch 是否足够取决于数据量（但训练集大小也未报告）；无早停、无 dropout/L2 等信息。

---

## 四、评估协议 — 泄漏风险分析

### ⚠️ 主要关注点

1. **测试集固定为 300 样本** — 没有说明这 300 个样本是最后 300 个交易日还是随机抽取。如果是随机抽，则存在未来信息泄漏（时间序列中随机抽样会让模型"看到"未来的统计信息）。

2. **无时间序列交叉验证** — 没有提及 walk-forward validation、rolling window 或 expanding window 等时间序列专用评估方法。

3. **无验证集** — 未提及使用验证集做早停或超参选择。训练集和测试集之外无独立验证数据。

4. **超参数报告不完整** — 批量大小、序列长度 L、隐藏层数均未报告。

5. **特征中包含 PE/PB/PS 等基本面指标** — 这些指标使用已发布的财务报表数据。如果财务数据发布日期与交易日期的对齐存在偏差，可能引入未来信息泄漏（look-ahead bias）。论文未讨论此问题。

**结论：** 评估泄漏风险较高，无法确认结果是泄漏-free 的。

---

## 五、报告指标

| 指标 | 公式（论文中） | 说明 |
|------|---------------|------|
| MSE | (1/n)∑(Ŷ - X)² | 标准 |
| RMSE | √MSE | 标准 |
| MAE | (1/n)∑|Ŷ - X| | 标准 |
| R² | **||Ŷ - X̄||² / ||X - X̄||²** | **公式错误/非常规** |

### ⚠️ R² 公式严重问题

标准 R² = 1 - ||X - Ŷ||² / ||X - X̄||²

论文中的 R² = ||Ŷ - X̄||² / ||X - X̄||²

这个公式**不是**标准 R²，而是"预测方差与实际方差的比值"，可以大于 1，不能为负。它衡量的是预测值的方差能否匹配真实值的方差，而不是预测的准确性。这是一个**严重错误**或误导性指标。

---

## 六、结果审查 — 论文的"优于所有方法"是否成立？

### 表 I：600036.SH（招商银行）

| 模型 | MSE ↓ | MAE ↓ | R² ↑ |
|------|-------|-------|------|
| **MambaStock** | **1.1514（最差）** | **0.8048（最差）** | **0.8873（最高）** |
| ARIMA-NN | 0.8813（最好） | 0.6869（最好） | 0.8577 |
| LSTM | 1.0229 | 0.7622 | 0.8230 |
| Transformer | 1.1185 | 0.7879 | 0.8757 |

**结论：MambaStock 在此股票上 MSE 比 ARIMA-NN 高 30.6%，MAE 高 17.2%，属于表现最差的模型。** R² 最高的同时 MSE 最高，数学上矛盾（除非使用非标准公式）。

### 表 II：601288.SH（农业银行）

- MambaStock MSE = 0.0006，而 LSTM/BiLSTM = 0.0005（MambaStock 更差）
- MambaStock MAE = 0.0182，而 LSTM = 0.0175，BiLSTM = 0.0170（MambaStock 更差）
- MambaStock R² = 0.9733（与 AttCLX 并列最高）

### 表 III：601328.SH（交通银行）

- MambaStock 在此股票上表现最好（MAE=0.0293 vs LSTM 0.0312，改善约 6.1%）

### 表 IV：601988.SH（中国银行）

- MambaStock MAE = 0.0135，而 BiLSTM = 0.0122（MambaStock 更差）
- MambaStock R² = 0.9590，AttCLX = 0.9588（几乎相同）

**汇总：**

| 指标 | MambaStock 胜出 | 持平 | 输掉 |
|------|----------------|------|------|
| MAE vs LSTM | 1/4 股票 (601328, +6.1%) | 0 | **3/4 股票 (-4.7% ~ -17.2%)** |
| MSE vs 最佳基线 | 1/4 股票 | 0 | **3/4 股票** |

---

## 七、MAE -18% vs LSTM 的声称可信吗？

**不可信。** 论文声称"MAE -18% vs LSTM"的描述未明确指向哪只股票/哪个实验。从实际数据看：

- 在 MambaStock 表现最好的 601328.SH 上，MAE 改善仅 **6.1%**（0.0312 → 0.0293），远不到 18%
- 在 600036.SH 上，MambaStock 的 MAE 甚至比 LSTM **差 5.6%**
- **没有在任何股票上实现 18% 的改善**

18% 这个数字的来源不明，可能是选择性报告某个特定子集或使用了不同的比较基准。**这是一个严重夸大的声称，与论文自身数据矛盾。**

---

## 八、论文的整体可信度评估

### 严重问题

| # | 问题 | 严重程度 |
|---|------|---------|
| 1 | **声称与数据矛盾**：论文声称"优于所有方法"，但表 I 显示 MambaStock 是最差的 | **严重** |
| 2 | **R² 公式错误**：使用了非标准公式，且与 MSE 结果矛盾 | **严重** |
| 3 | "MAE -18% vs LSTM" 无事实依据 | **严重** |
| 4 | 仅 4 只银行股，行业集中，无法泛化 | **高** |
| 5 | 无验证集，无泄漏防护说明 | **高** |
| 6 | R² 值在表 I 中与 MSE 不一致（高 MSE 却最高 R²），暗示计算或报告的 R² 有误 | **严重** |
| 7 | 批量大小、序列长度、训练集大小等关键实验参数未报告 | **中** |
| 8 | 学习率 0.01 过高且无调度 | **中** |
| 9 | 状态维度 N=16 非常小（标准 Mamba 论文通常用 N=16-64 但配合大模型） | **中** |
| 10 | 无代码复现验证（仓库 github.com/zshicode/MambaStock 未检查） | **中** |

### 总体评估

**MambaStock 论文目前不应被信任。** 主要问题不是方法本身的可行性，而是：
1. 实验结果的声称与实际数据严重矛盾
2. 指标计算公式存在错误
3. 评估协议不完整，有泄漏风险
4. 实验规模极小（4 只银行股），无法支持"泛化优越性"的结论

一个负责任的结论是：Mamba 在股票预测中可能有一定潜力（尤其是在 Mamba 架构的理论优势方面），**但该论文的实验结果不足以支持其声称的优越性**。需要在大规模、多样化数据集上进行独立复现和 walk-forward 验证才能确认。

---

## 九、相关背景：对 Glaubenskrieg 项目的启示

从同时阅读的三篇股票预测综述论文（StockForecasting-Survey, StockMarket-DLSurvey, ML-FinancialForecasting）中可以提取以下重要启示：

### 综述论文关键发现

| 来源 | 关键观点 |
|------|---------|
| StockForecasting-Survey (2009) | 100+ 论文的经典综述。NN 和神经模糊是主流，输入变量通常 4-10 个，最常见使用 OHLCV |
| StockMarket-DLSurvey (2023, 94 papers) | 分类法：任务分 4 类（价格预测、走势预测、投资组合、交易策略）；模型分 RNN/CNN/GNN/Transformer/RL；大多数研究仅使用市场数据 |
| ML-FinancialForecasting (2020, 124 papers) | 7 种数据类型分类；强调泄漏问题（基本面数据的报告日期偏差）；日频预测占 85%；注意评价指标包括**盈利能力**（不仅仅是预测误差） |

### 对 Glaubenskrieg 项目的参考

- **MambaStock 的不可信不否定 Mamba 方法的潜力**，但本项目已有 TimeGate + Mamba 结构，不需要依赖 MambaStock 的结果
- 评估泄漏是股票预测论文的通病，**Glaubenskrieg 项目的 walk-forward validation 设计明显优于 MambaStock**
- MambaStock 证明了 Mamba 可以处理 15 维多变量输入，这与 Glaubenskrieg 的 multi-asset 结构相关
- 但 MambaStock 的状态维度 N=16 远小于 Glaubenskrieg 的 state_dim（目前 16，计划扩展到 24），这意味着 Glaubenskrieg 的 Mamba 层容量更大

---

*分析完成于 2026-06-06*
