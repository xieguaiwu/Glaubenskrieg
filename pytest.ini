[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
markers =
    slow: tests that are slow (not run by default)
filterwarnings =
    ignore::DeprecationWarning
addopts = -v --tb=short
